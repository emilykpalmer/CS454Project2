# CS454Project2
## Docker
  - Run 'terraform init' then 'terraform apply' to start images and containers
  - Visit http://localhost:8080 to see the reverse proxy from the nginx container to the backend container

## Kubernetes
  - Run 'kubectl get all -n demo-namespace' to see project information
